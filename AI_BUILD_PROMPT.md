# 钢贸企业三维经营分析与诊断系统 · AI 开发主提示词（Cursor 里程碑驱动）

> 你是资深全栈架构师 + 高级工程师。本文件是本项目的**开发总纲与唯一执行入口**。
> 当用户输入「**请开始编码**」时，你必须**从里程碑 M0 开始，按顺序自动执行到 M9 全部完成**，无需用户逐步下达指令（除非用户明确喊停或调整）。

---

## 0. 三大「唯一真源」（SSOT）—— 任何实现以它们为准，禁止臆造/删减

| 真源 | 位置 | 作用 |
|---|---|---|
| **高保真原型** | `prototype/*.html` + `prototype/assets/` | UI 布局、组件、图表类型、交互、文案、配色的**唯一视觉与交互标准**（100% 还原） |
| **PRD** | `prototype/prd.html` | 功能范围、数据模型/ER、状态机、流程图、时序图、平台架构、信息架构、需求清单、非功能需求、迭代规划 |
| **指标与算法字典** | `prototype/algorithms.html` | 所有指标/能力分析的**计算口径、公式、阈值、权重**（100% 实现，阈值做成可配置） |

**开工前必读**：完整阅读上述三处内容 + `prototype/assets/js/data.js`（字段与数据结构参考）、`prototype/assets/js/common.js`（布局/图表主题）、`prototype/assets/css/style.css`（设计规范：色板/圆角/阴影/间距）。

> 说明：`design.html / prd.html / algorithms.html / rollout.html` 属**设计文档页**，是开发依据，不必做成终端用户功能（可作为系统内「帮助/口径说明」）；其余页面为**产品功能页面，必须逐一还原实现**。

---

## 1. 铁律（贯穿所有里程碑，违背即返工）

1. **页面 100% 还原**：路由、栅格布局、侧边栏/顶栏、KPI 卡、图表（类型/系列/配色/图例/tooltip/标注）、表格列、筛选器、标签页、分页、按钮、空态、文案，均与原型一致。设计 token 严格对齐 `style.css`（主色 `#2563eb`、成功 `#16a34a`、警告 `#d99e00`、危险 `#dc2626`、圆角 14px 等）。
2. **功能 100% 不缺**：PRD 第 13 节「功能需求清单」中每一行都必须实现并通过验收；不得以"简化/后续再做"为由删减 P0/P1。
3. **逻辑 100% 完整**：算法字典中每个公式、分层、阈值、权重、预警规则、状态机流转、审批链、数据域规则、Token 计费口径全部实现，且**阈值/权重可后台配置**（对应原型的可配置面板）。
4. **数据真实**：所有指标从真实数据源经计算引擎产出；`data.js` 的 mock 仅用于理解字段结构与图表数据形态，不得写死进生产逻辑。
5. **里程碑闭环**：每个里程碑必须「可运行 + 有单元/接口测试 + 通过本里程碑验收清单 + 更新文档 + 提交」后，才进入下一里程碑。
6. **歧义处理**：遇到不明确处，先在三大真源中检索；仍不明确则**记录假设（写入 `docs/decisions.md`）并按合理默认继续**，不得阻塞停工。
7. **每个里程碑结束输出**：完成报告（做了什么）+ 自检清单（逐条对照验收标准打勾）+ 已知问题 + 下一里程碑计划，然后**自动继续**。

---

## 2. 技术栈（默认方案；若与贵司平台既有栈冲突，以平台为准并在 `docs/decisions.md` 记录）

- **前端**：Vue 3 + TypeScript + Vite + Pinia + Vue Router + Element Plus + ECharts 5 + (PRD 图用 mermaid)。需支持**微前端挂载**（qiankun/wujie）以嵌入公司平台。
- **后端**：Spring Boot 3 + MyBatis-Plus + Redis（缓存/诊断结果缓存）+ ShardingSphere（分库分表）。（或 NestJS / FastAPI，按平台栈）
- **任务调度**：XXL-Job / PowerJob（DAG 依赖、重试、补数、分片、分租户）。
- **数据源适配**：SQL Server、MySQL（JDBC 只读账号）、API（REST，Bearer/OAuth2/AppKey）；统一抽象 `DataSourceConnector` 接口 + 字段映射到 36 项标准字段。
- **鉴权/租户**：**不自建登录**，复用平台统一基础接口（SSO Token 校验、租户上下文、组织/角色同步）。所有请求携带并校验 `tenantId` + 用户身份；数据域按组织树注入行级过滤。
- **AI**：对接 OpenAI 兼容/通义千问/文心 企业版；Prompt 模板内置（见 ai-diagnosis 原型）；Token 计费与缓存（7 天）。
- **工程规范**：ESLint/Prettier、后端 Checkstyle、统一异常与返回体、OpenAPI(Swagger)、单元测试（前端 Vitest、后端 JUnit）、Git 提交规范（Conventional Commits）。

---

## 3. 里程碑计划（「请开始编码」后按序执行 M0→M9）

> 每个里程碑标注：**目标 / 依赖真源 / 交付物 / 验收标准**。

### M0 · 工程脚手架与规范
- 目标：搭前后端工程、目录结构、代码规范、CI、统一返回体/异常/日志、OpenAPI、基础布局（侧边栏+顶栏，还原 `common.js` 导航结构与 `style.css` 主题）、路由骨架（对应 16 页面/功能页）。
- 交付：可运行的空壳应用（登录走平台 SSO 桩），侧边栏分组与原型一致（经营总览/三维分析/智能诊断/系统与权限）。
- 验收：`npm run dev` 与后端可启动；布局像素对齐原型；路由可跳转；lint/测试脚手架通过。

### M1 · 数据底座 & 数据源管理（真源：PRD §4/§9、datasource.html、algorithms §0）
- 目标：`DataSourceConnector`（SQLServer/MySQL/API 三实现）+ 连接测试 + 字段映射（→36 标准字段）+ 同步（全量/增量/CDC）+ 落 ODS→清洗→维度聚合→**指标宽表**（对应 6 张模型表：原始明细/市场汇总/客户分层/客户四象限/员工/AI诊断）。
- 通用口径实现：单笔毛利/业务净利/单吨净利、现款/赊销/逾期判定、加权账期、活跃客户、全局中位数 X0/Y0。
- 交付：数据源管理页（列表/新增表单按类型切换字段/测试/同步/字段映射表），跑批产出宽表。
- 验收：三类源均可连通并同步；字段映射可配置；宽表指标与算法字典口径一致（对拍单测）。

### M2 · 平台接入 + 角色权限（真源：roles.html、PRD §8、datasource §③）
- 目标：SSO Token 校验中间件、租户上下文注入、组织/角色同步；**RBAC**（角色-权限点）+ **数据域**（组织树行级权限：可见客户=归属销售∈下属集合，递归；上级继承下级；同级隔离）+ **字段级脱敏**（成本价/货源等）+ **分级审批链**（分配/回收/授信/套餐）+ **操作审计**。
- 覆盖 7 角色（总裁/运营经理/部门经理/主管/组长/销售员/助理）与功能权限矩阵（roles.html 第 4 节 10×7）。
- 验收：不同角色登录后数据范围、可用功能、审批与脱敏均符合矩阵；越权被拦截；操作留痕可查。

### M3 · 指标计算引擎 & 规则引擎（真源：algorithms.html 全部、churn.html 可配置模型）
- 目标：实现全部算法——
  - 市场：区域指标库、四象限判定、品类/产地/计价、行业指标、机会/衰退预警规则。
  - 客户：基础价值、**购货频率 5 指标（含 AvgT/CV/RecentT）**、频率分层、**走量/盈利四象限（X0/Y0）**、**四维潜力打分（40/25/20/15）**、**流失评分（S1-S4 加权 + 硬预警标签）**、**复购预测（上次下单+AvgT，dueIn，复购概率）**、跟进优先级。
  - 员工：业绩/资源结构/集中度/公平度/盘活、四象限、六维雷达评分。
  - 客户分级动态升降级、健康分。
- **所有阈值/权重存配置表，提供可视化配置面板**（流失模型四因子、回收规则、全局阈值），实时重算——对应 churn/assign 原型交互。
- 验收：每个模型有单测对拍（给定输入→期望标签/分数）；配置修改后结果实时联动；与原型可配置面板行为一致。

### M4 · 三维分析看板 + 总裁驾驶舱（真源：dashboard/market/customer/employee.html）
- 目标：像素级还原四页所有图表与交互——
  - 驾驶舱：6 KPI、三栏(市场/客户/员工四象限散点)、AI 综合诊断、今日重点动作、快捷工作台入口。
  - 市场：区域地图(china GeoJSON, 失败降级条形)、四象限、双轴趋势、堆叠结构、排名、结算饼；品类气泡/瀑布/趋势/产地；行业面积/雷达；AI 诊断卡 + 资源倾斜方案表。
  - 客户：分级漏斗、走量/盈利矩阵、毛利贡献饼、频率分层/间隔分布/单客趋势、**到期复购预测卡**、采购波动预警、账期风险散点、分配对比/达标热力、客户分层表。
  - 员工：业绩排名、潜力四象限、资源分布、公平度、集中度、六维雷达(可切换)、逾期预警、全员表、**差异化管理表 + 客户资源回收清单**。
- 验收：图表类型/系列/配色/tooltip/标注与原型一致；标签页切换图表正确 resize（参考 common.js 懒渲染修复）；数据来自引擎按数据域过滤。

### M5 · 落地执行工作台（真源：customer-list/assign.html）
- 目标：
  - **客户查询工作台**：多维筛选(含复购信号)、快捷标签、排序、分页(20/50/100)、跨页多选、批量分配/设优先级/加入唤醒、导出 CSV、结果概览与分布图。
  - **跟进分配·我的客户**：智能分配建议(能力×负载×潜力)、负载均衡、**可配置回收&分配规则面板**(实时重算命中)、**到期复购催单队列(一键生成任务)**；销售视角"我的客户"任务(逾期/今日/本周分组、复购催单/推单/预热、记录跟进、达标率)。
- 验收：筛选/排序/分页/批量/导出全部可用且按数据域限制；分配/回收走审批;催单任务生成并按角色分发。

### M6 · AI 诊断 + Token 计费中心（真源：ai-diagnosis/billing.html、algorithms §5）
- 目标：
  - 规则预警(引擎生成，按字节计 Token) + 大模型三段式深诊(仅高风险/机会触发) + 交互问答 + 缓存(7 天，新单据失效)；内置钢贸专属 Prompt 模板与分支。
  - **统一 Token 计费**：常规报表按输入/输出字节折算、AI 按大模型 token；**计费中心**(用量总览、趋势/构成、按功能/用户/报表类型排行、账单导出、余额预警、套餐管理：预存/按量/包月)。
- 验收：Token 计量与账单准确；缓存命中不重复扣费；余额预警触发；套餐切换生效；私有化(自备密钥)/云 SaaS 两种计费模式可切换。

### M7 · 定时任务/自动化 + 消息推送（真源：jobs.html、PRD §10）
- 目标：XXL-Job 编排**每日数据管道 DAG**（同步→清洗→指标→打标签→升降级→AI批量→分配/生成任务→推送），失败重试+告警+幂等补数+分租户；作业调度监控页(DAG图/执行时间线甘特/调度表/日志)；企微/钉钉按销售分发报表与预警。
- 验收：DAG 依赖与顺序正确；失败重试与告警生效；可手动执行/补数(需权限留痕)；监控页展示真实运行状态。

### M8 · 联调 / 测试 / 性能 / 安全
- 目标：端到端联调；单测覆盖核心算法与权限；性能(万级客户跑批与秒开、分页/预计算/缓存)；安全(越权/注入/脱敏/审计)；多租户隔离验证。
- 验收：覆盖率达标；关键页 P95 加载<2s；无高危漏洞；租户/数据域隔离通过测试。

### M9 · 交付 / 部署 / 文档
- 目标：容器化、CI/CD、环境配置、平台挂载(微前端)联调、部署手册、运维手册、验收报告。
- 验收：可在平台内作为模块打开并通过 SSO 登录；全部里程碑验收清单通过。

---

## 4. 页面还原对照清单（功能页 → 路由 → 关键点）

| 原型页 | 路由 | 关键组件/图表 | 里程碑 |
|---|---|---|---|
| dashboard.html | /dashboard | 6KPI·三栏四象限散点·AI综合诊断·动作队列·快捷入口 | M4 |
| market.html | /market | 地图/四象限/双轴/堆叠/排名/饼·气泡/瀑布/折线/产地·面积/雷达·AI卡·资源倾斜表 | M4 |
| customer.html | /customer | 漏斗/矩阵/饼·频率分层/直方/单客/复购预测·预警/风险散点·分配对比/热力·分层表 | M4 |
| employee.html | /employee | 排名/四象限/堆叠/公平度/集中度/六维雷达(切换)/逾期·画像表·差异化管理表·回收清单 | M4 |
| customer-list.html | /customers | 多维筛选/快捷标签/排序/分页/多选/批量/导出/分布图/复购信号列 | M5 |
| assign.html | /assign | 智能分配建议/负载/回收规则配置/复购催单队列·我的客户任务(分组/记录跟进) | M5 |
| ai-diagnosis.html | /ai | 规则预警区/AI三段诊断/问答/Token计费模型/账单/Prompt | M6 |
| churn.html | /churn | 可配置四因子流失评分/分布/频率分层/因子权重/预警表/销售分发 | M3+M5 |
| billing.html | /billing | 用量总览/趋势构成/多维排行/账单导出/余额预警/套餐管理 | M6 |
| roles.html | /system/roles | 组织层级图/角色定义/数据范围/功能权限矩阵/监管/审批流 → 权限管理后台 | M2 |
| datasource.html | /system/datasource | 数据源列表/新增(类型切换)/字段映射/同步·平台接入架构/SSO时序 | M1+M2 |
| jobs.html | /system/jobs | 数据管道DAG/执行时间线甘特/调度表/容错机制/执行日志 | M7 |

---

## 5. 算法实现对照（全部见 `prototype/algorithms.html`，逐条实现 + 单测对拍）

- §0 通用口径：单笔毛利/业务净利/单吨净利、现款/赊销/逾期、加权账期、活跃客户、X0/Y0。
- §1 市场：区域规模/盈利/风险/结构/趋势指标、四象限标签公式、品类/产地/计价、行业雷达、机会/衰退预警。
- §2 客户：基础价值 / 购货频率 5 指标(AvgT·CV·RecentT·增量·广度) / 频率分层 / 走量-盈利四象限(X0,Y0) / 四维潜力打分(0.4/0.25/0.2/0.15) / 流失评分(S1×.4+S2×.3+S3×.2+S4×.1 + 硬预警) / **复购预测(上次下单+AvgT, dueIn 分层, 复购概率)** / 跟进优先级。
- §3 员工：业绩/资源结构/集中度(≥0.7)/盘活/公平度、四象限、六维雷达。
- §4 阈值与权重汇总：全部入配置表，可视化配置。
- §5 Token 计费与触发节流：报表字节折算、AI 触发条件、缓存 7/30 天。

---

## 6. 「请开始编码」触发协议（重要）

当用户输入「**请开始编码**」（或等义指令）时：
1. 先输出一句确认 + M0 计划，然后**立即开始 M0**；
2. 每完成一个里程碑：提交代码（Conventional Commits）→ 输出「完成报告 + 验收自检清单(逐条✅) + 已知问题」→ **自动进入下一里程碑**；
3. 持续执行直至 M9 完成；除非用户喊「暂停/调整」；
4. 全程遵守第 1 节铁律：不缺功能、不减逻辑、还原 UI、真实数据、里程碑闭环；
5. 任何被平台约束（栈/接口/鉴权）替换的决策，写入 `docs/decisions.md` 并继续。

> 交付目标：一套可在公司平台内作为模块运行、市场/客户/员工三维分析 + AI 诊断 + 全量 Token 计费 + 角色权限 + 多 ERP 数据源 + 定时自动化 的**生产级系统**，与本仓库原型 100% 一致、功能 100% 完整、逻辑 100% 正确。
